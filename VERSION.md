# VERSION

이 문서는 `v1.0` 서비스 시작 이후 주요 기능과 변경 사항을 기록합니다.
앞으로도 `v1.x` 범위에서 기능 추가와 개선 내역을 계속 추적합니다.

## v1.3.0

- 지출결의서 작성 화면에 `기존 지출결의서에서 항목 불러오기` 기능 추가
- `지출결의서 작성 가이드 보기` 아래에 불러오기 버튼과 전용 선택 모달 추가
- 내가 작성한 기존 결의서만 복사 소스로 조회하도록 권한 구조 유지
- 선택한 결의서의 항목, 적요, 월분, 수량, 단가, 금액, 계좌정보를 새 결의서에 복사
- 복사 적용 시 `결의일자`와 `청구일자`는 오늘 날짜로 초기화
- 영수증은 복사 대상에서 제외하고, 모달과 적용 후 안내 문구로 복사 범위를 명확히 고지
- 복사 소스 전용 API `copy_sources`, `copy_source_detail` 추가
- 배포 API가 아직 없거나 호출에 실패하는 경우를 대비해, 프론트에서 내 결의서를 직접 조회하는 fallback 추가
- 지출결의서 제출 전에 최종 확인 팝업 추가
- 영수증 첨부 정책을 `파일당 최대 1MB`, `최대 15개`, `11개 이상 분리 제출 권장` 기준으로 정리
- 영수증 첨부 시 허용 형식/용량/개수 제한 안내 문구 추가
- 업로드 실패 시 어떤 파일이 왜 실패했는지 사용자에게 표시하도록 개선
- 일부 영수증만 업로드 성공한 상태에서 오류가 나면, 성공분을 정리해 데이터 불일치가 남지 않도록 보강
- 로그인 이후 문의 담당자와 연락처를 안내하는 공지 팝업 추가

## v1.2.0

- 지출결의서 보기 API를 목록 조회와 상세 조회로 분리하고 서버 기준 페이지네이션 도입
- PC와 모바일 지출결의서 보기 화면에 페이지당 5건 기준 페이지네이션 적용
- 페이지 이동과 상세 진입 시 로딩 상태가 더 분명하게 보이도록 UX 보강
- 지출결의서 목록 캐시, 상세 캐시, 캐시 무효화 범위 확장으로 반복 조회 체감 속도 개선
- 성능 측정 로그를 추가해 목록 표시/새로고침 흐름을 추적할 수 있도록 보강
- 내 정보 저장 시 이름, 소속, 연락처 수정 내용이 실제 사용자 정보와 즉시 동기화되도록 수정
- PC 화면 기준 공통 콘텐츠 폭을 넓혀 로그인, 요약 보기, 상세내역 보기, 지출결의서 작성, 자유게시판, 내 정보 화면의 불필요한 좌우 여백 축소
- 요약 보기 화면에서 조회하기 버튼을 필터 옆으로 이동하고, 사용하지 않는 주간보고서/월간보고서 버튼을 숨김 처리
- 요약 보기 화면을 한눈에 주요 정보를 파악할 수 있는 대시보드형 구조로 재구성

## v1.1.3

- PC 지출결의서 보기 화면의 본문 폭과 컬럼 비율을 조정해 작성일자, 작성자명, 주요 내역이 불필요하게 줄바꿈되지 않도록 개선
- 처리현황 체크 항목에서 체크한 사용자의 이름을 기본 화면에서는 숨기고, 마우스 오버 시 툴팁으로만 보이도록 정리
- 처리현황 각 체크 항목을 변경할 때 확인 팝업을 먼저 띄워 실수로 체크/해제되는 상황을 줄임
- 지출결의서 목록과 상세 조회에 작성자명을 서버에서 안전하게 조합해 내려주는 읽기 전용 API를 추가
- 회계팀 계정에서 작성자명이 `작성자 미상`으로 보이던 문제 수정

## v1.1.2

- PC 지출결의서 인쇄 시 본문 다음 페이지부터 첨부 영수증도 함께 출력되도록 확장
- 영수증 이미지 비율에 따라 가로형, 세로형, 정사각형 기준으로 인쇄 크기를 조절해 종이 낭비를 줄이도록 보정
- 인쇄용 영수증 영역에서 파일명은 제외하고 이미지만 출력되도록 정리
- 영수증 이미지 준비가 끝난 뒤에만 인쇄 버튼이 동작하도록 보강

## v1.1.1

- Vercel Web Analytics 패키지 설치 및 전역 Analytics 컴포넌트 연결
- Vercel Speed Insights 패키지 설치 및 전역 Speed Insights 컴포넌트 연결

## v1.1.0

- 자유게시판 메뉴 추가 및 로그인한 전체 사용자를 대상으로 게시글 조회/작성 기능 도입
- 자유게시판 게시글 상세 보기, 본인 글 수정/삭제, 마스터 계정의 전체 글 삭제 권한 추가
- Supabase 자유게시판 테이블과 RLS 정책 스크립트 추가

## v1.0.1

- 지출결의서 조회 화면의 PC 문서 형식을 실제 사용 문서 형식에 맞게 보정
- 지출결의서 하단 결재/서명 표시와 비고 영역 정리
- 지출결의서 조회 화면에 인쇄 기능 추가
- 모바일에서 지출결의서 내용을 더 읽기 쉽게 카드형 상세 UI 추가
- PC 인쇄용 지출결의서 문서 형식을 결재 영역 기준으로 다시 정리
- 모바일 전용 지출결의서 상세에서 확인 정보 영역을 줄이고 작성자 정보를 하단 영역으로 이동
- 모바일 전용 지출결의서 상세 UI를 서비스 기본 색상 흐름에 맞게 조정

## v1.0

- 정식 서비스 시작
- 사용자 로그인 및 승인 기반 접근 제어 적용
- 요약 보기, 상세 보기, 지출결의서 작성/조회, 사용자 권한 관리 화면 운영 시작
- 보고서 조회 및 기본 업무 흐름 사용이 가능한 상태로 배포
